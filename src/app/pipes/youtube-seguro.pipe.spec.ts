import { YoutubeSeguroPipe } from './youtube-seguro.pipe';

describe('YoutubeSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
